# 待办事项

## 自定义完全模拟缓存

- [ ] 梳理当前 `anthropic/cache_metering` 的模拟缓存逻辑和计费字段。
- [ ] 研究 junnl 的模拟缓存设计，确认缓存键、TTL、命中规则、缓存创建/读取 token 的计算方式。
- [ ] 将 junnl 的模拟缓存能力迁移为本项目可配置模块。
- [ ] 支持按模型、账号、客户端 Key、请求内容维度自定义缓存策略。
- [ ] 在 Admin UI 暴露模拟缓存开关、TTL、命中统计和清理操作。
- [ ] 增加单元测试：缓存命中、缓存过期、不同 key 隔离、流式/非流式一致性。

## 模仿 junnl 并发调度中心

- [ ] 梳理当前账号调度、失败切换、额度耗尽、冷却恢复的实现。
- [ ] 研究 junnl 的并发调度中心：队列模型、账号锁、并发上限、请求排队、失败回退。
- [ ] 设计统一调度中心，集中处理账号选择、并发限制、冷却、重试和熔断。
- [ ] 支持按账号、分组、模型、客户端 Key 配置并发上限。
- [ ] 支持请求级排队超时和快速失败策略。
- [ ] 在 Admin UI 增加调度中心状态面板：运行中请求、排队请求、账号占用、失败原因。
- [ ] 增加压力测试脚本，验证高并发下不会重复占用同一账号或突破并发上限。

